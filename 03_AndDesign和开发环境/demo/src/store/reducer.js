const defaultState = {
    inputValue: 'Write Something',
    list: [
        '1，早上开晨会明确代码任务',
        '2，上午正常开发',
        '3，中午用餐，午休'
    ]
}

var result = (state = defaultState, action) => {
    return state
}

export default result